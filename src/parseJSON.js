// this is what you would do if you were one to do things the easy way:
// var parseJSON = JSON.parse;

// but you're not, so you'll write it from scratch:
var parseJSON = function(json) {
  // your code goes here

  

  // var index = 0;
  
  // if (json.length === index)

  // string
  // number
  // object
  // array
  // true
  // false
  // null
};

var value = function(index, json) {

  // if (json[index] === )
};

//we see a quote
//we go to string function while increasing index
// if(JSON[index] === '"')
// var str = ";
// while (JSON[index] !== '"') {
//  str = str.concat(json[index]);
//  index++;
//}